# Gin-Vue-Admin Global Configuration

# casbin configuration
casbin:
    model-path: './resource/rbac_model.conf'

# jwt configuration
jwt:
    signing-key: 'qmPlus'

# mysql connect configuration
mysql:
    username: root
    password: 'maikangroot'
    path: '106.53.124.190:3306'
    db-name: 'admin'
    config: 'charset=utf8&parseTime=True&loc=Local'
    max-idle-conns: 10
    max-open-conns: 10
    log-mode: false

#sqlite 配置
sqlite:
    path: db.db
    log-mode: true
    config: 'loc=Asia/Shanghai'

# oss configuration

# 请自行七牛申请对应的 公钥 私钥 bucket 和 域名地址
qiniu:
    access-key: '9SyYfyDjCL40pW8HeEg7kdE8TspTDcVkNLD87pMJ'
    secret-key: 'uWDTlo-06e_uXlKfGvGou77WNNLjw7AOB6gtI9Ol'
    bucket: 'mk-admin'
    img-path: 'http://img.mkhealth.club/'
# redis configuration
redis:
    addr: '106.53.124.190:6979'
    password: ''
    db: 15

# system configuration
system:
    use-multipoint: false
    env: 'public'  # Change to "develop" to skip authentication for development mode
    addr: 8888
    db-type: "mysql"  # support mysql/sqlite

# captcha configuration
captcha:
    key-long: 6
    img-width: 240
    img-height: 80

# logger configuration
log:
    prefix: '[GIN-VUE-ADMIN]'
    log-file: true
    stdout: 'DEBUG'
    file: 'DEBUG'